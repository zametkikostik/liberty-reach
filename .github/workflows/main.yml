📊 Статус:
✅ Релиз создан:
Тег: v1.0.0
URL: https://github.com/zametkikostik/liberty-reach/releases/tag/v1.0.0
❌ Workflow файлы НЕ добавлены:
Токен не имел прав workflow
Сборки НЕ запустились автоматически
APK/MSI/DMG/AppImage не собраны
🔧 Как исправить (добавить workflow вручную):
Вариант 1: Через GitHub UI (Самый простой)
Откройте: https://github.com/zametkikostik/liberty-reach/actions
Нажмите: “set up a workflow yourself”
Вставьте это содержимое:
name: Build Android APK

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v3
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
      - name: Build APK
        working-directory: apps/mobile/android
        run: |
          chmod +x build-simple-apk.sh
          ./build-simple-apk.sh
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: liberty-reach-debug
          path: apps/mobile/android/app/build/outputs/apk/debug/app-debug.apk
